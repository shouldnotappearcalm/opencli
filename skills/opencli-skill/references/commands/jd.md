# jd

## Commands

### item
- Purpose: Read JD product details (price, images, and specs)
- Args:
  - `sku`(required); 商品 SKU ID（如 100291143898）
  - `images`(optional; type: int; default: 10); AVIF 图片数量上限（默认10）
- Usage: `opencli jd item [options] -f json`
