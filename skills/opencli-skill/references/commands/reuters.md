# reuters

## Commands

### search
- Purpose: Search Reuters news
- Args:
  - `query`(required); Search query
  - `limit`(optional; type: int; default: 10); Number of results (max 40)
- Usage: `opencli reuters search [options] -f json`
